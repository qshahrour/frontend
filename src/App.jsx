import React from "react";

export default function App() {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h1>Hello from Vite + React + Kubernetes!</h1>
      <p>This is a minimal React app ready for Docker and Ingress.</p>
    </div>
  );
}
